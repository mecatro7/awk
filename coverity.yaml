capture:
  build:
    clean-command: make clean
    build-command: make
  compiler-configuration:
    cov-configure:
    - [ --template, --compiler, cc, --comptype, gcc ]
analyze:
  coding-standards:
    misrac2012:
      pre-canned: all

commit:
  connect:
    url: http://azure-coverity2:8080
    stream: awk
